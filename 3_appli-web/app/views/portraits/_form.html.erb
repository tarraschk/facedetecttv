<%= form_for(portrait) do |f| %>
  <% if portrait.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(portrait.errors.count, "error") %> prohibited this portrait from being saved:</h2>

      <ul>
      <% portrait.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :filename %>
    <%= f.text_field :filename %>
  </div>

  <div class="field">
    <%= f.label :image_id %>
    <%= f.text_field :image_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
